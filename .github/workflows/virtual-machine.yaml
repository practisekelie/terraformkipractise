name: "VM-creation"

on:
  workflow_dispatch:  

permissions:
    id-token: write
    contents: read
jobs:
  TerraformInit: 
    name: Init and plan
    runs-on: vm-runner-1
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0 

      - name: Install Terraform
        uses: little-core-labs/install-terraform@v2.0.0
        with:
          version: 1.8.5
